{
  "name": "food-ordering-system",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "npm install",
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "tsc -b",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --passWithNoTests",
    "test:ci": "jest --ci --passWithNoTests --maxWorkers=2",
    "test:integration": "jest --config jest.integration.config.js",
    "new:service": "node ./scripts/new-service.js",
    "dev:auth": "cd services/auth && npm install && npm run dev",
    "dev:order": "cd services/order && npm install && npm run dev",
    "dev:restaurant": "cd services/restaurant && npm install && npm run dev",
    "dev:kitchen": "cd services/kitchen && npm install && npm run dev",
    "dev:payment": "cd services/payment && npm install && npm run dev",
    "dev:delivery": "cd services/delivery && npm install && npm run dev",
    "dev:notification": "cd services/notification && npm install && npm run dev",
    "dev:api-gateway": "cd services/api-gateway && npm install && npm run dev",
    "prisma": "echo 'Prisma clients are auto-generated on npm install via postinstall hooks'"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.8",
    "@types/bcrypt": "^6.0.0",
    "@types/express": "5.0.6",
    "@types/jest": "^29.5.0",
    "@types/swagger-jsdoc": "^6.0.4",
    "@types/swagger-ui-express": "^4.1.8",
    "@types/uuid": "^10.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "husky": "^8.0.0",
    "jest": "^29.5.0",
    "jest-extended": "^4.0.0",
    "lint-staged": "^13.0.0",
    "prettier": "^2.0.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.19.1",
    "bcrypt": "^6.0.0",
    "express-rate-limit": "^8.2.1",
    "http-proxy-middleware": "^3.0.5"
  }
}
