{
  "name": "service-delivery",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "amqplib": "^0.10.0",
    "dotenv": "^16.4.0",
    "express": "^4.18.2",
    "mongodb": "^6.5.0"
  }
}

